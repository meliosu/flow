import runtime;

import nemo;

main() {
    program = "
        var a: [int];
        var b: int;
        {
            a := [1];
            b := app(a, 0);
        }
    ";

    ast = parseAst(
        program
    );

    println(ast);

    if (isValidProgram(ast)) {
        println("valid nemo");
    } else {
        println("error!");
    }
}