import runtime;

import nemo;

main() {
    program = "
        var a: int;
        var b: int;
        {{
            {
                a := 1;
            } U {
                b := 2;
            }
        }}*
    ";

    ast = parseAst(
        program
    );

    println(ast);
}